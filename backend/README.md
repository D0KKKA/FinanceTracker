# Finance Tracker Backend API

NestJS приложение для управления личными финансами.

## 🚀 Запуск

```bash
# Установка зависимостей
npm install

# Запуск в режиме разработки
npm run start:dev

# Сборка проекта
npm run build

# Запуск продакшн версии
npm run start:prod
```

## 📊 API Endpoints

### Транзакции
- `GET /api/transactions` - получить все транзакции
- `POST /api/transactions` - создать транзакцию
- `GET /api/transactions/:id` - получить транзакцию по ID
- `PATCH /api/transactions/:id` - обновить транзакцию
- `DELETE /api/transactions/:id` - удалить транзакцию
- `POST /api/transactions/sync` - синхронизировать транзакции

### Категории
- `GET /api/categories` - получить все категории
- `POST /api/categories` - создать категорию
- `GET /api/categories/:id` - получить категорию по ID
- `PATCH /api/categories/:id` - обновить категорию
- `DELETE /api/categories/:id` - удалить категорию
- `POST /api/categories/seed` - создать дефолтные категории

### Настройки
- `GET /api/settings` - получить все настройки
- `GET /api/settings/default` - получить дефолтные настройки
- `POST /api/settings` - создать настройки
- `GET /api/settings/:id` - получить настройки по ID
- `PATCH /api/settings/:id` - обновить настройки
- `DELETE /api/settings/:id` - удалить настройки

## 🗄️ База данных

Используется SQLite база данных (`finance.db`) с автоматической синхронизацией схемы.

## 🔧 Технологии

- **NestJS** - фреймворк для Node.js
- **TypeORM** - ORM для работы с базой данных
- **SQLite** - база данных
- **class-validator** - валидация данных
- **class-transformer** - трансформация данных

## 📝 Структура проекта

```
src/
├── controllers/     # Контроллеры API
├── services/        # Бизнес-логика
├── entities/        # Сущности базы данных
├── dto/            # Data Transfer Objects
├── app.module.ts   # Главный модуль
├── app.service.ts  # Главный сервис
└── main.ts         # Точка входа
```

## 🌐 CORS

Настроен CORS для работы с фронтендом на портах 3000 и 3001.
